"""Minimal stub — weather monitor doesn't use the full strategy base class."""

from __future__ import annotations

from abc import ABC, abstractmethod


class StrategyBase(ABC):
    """Stub for strategy base class."""
    pass
